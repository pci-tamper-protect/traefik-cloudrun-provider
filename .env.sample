# Traefik Cloud Run Provider Configuration
# Copy this file to .env and fill in your values
#
# ============================================
# Advanced: Encrypted Secrets with dotenvx
# ============================================
# For encrypted secrets and .env.vault support, use dotenv-org/godotenvvault:
#   1. Replace import: github.com/joho/godotenv → github.com/dotenv-org/godotenvvault
#   2. Use dotenvx CLI for encryption: https://dotenvx.com
#   3. Supports .env.vault (encrypted) and .env.me (local encrypted secrets)
#
# Learn more: https://github.com/dotenv-org/godotenvvault

# ============================================
# Required Configuration
# ============================================

# Primary GCP project ID (required)
LABS_PROJECT_ID=my-project-prod

# GCP region for Cloud Run services (default: us-central1)
REGION=us-central1

# Environment name (stg, prod, dev)
ENVIRONMENT=prod

# ============================================
# Optional Configuration
# ============================================

# Secondary GCP project ID (optional)
# Useful for multi-project architectures
# HOME_PROJECT_ID=my-home-prod

# ============================================
# Logging Configuration
# ============================================

# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO

# Log format: text, json (default: text)
# Use 'json' for Cloud Logging compatibility
LOG_FORMAT=text

# ============================================
# Development Mode
# ============================================

# Enable ADC fallback for local development (default: auto-detected)
# Set to 'true' when running locally without metadata server
# Cloud Run environment is auto-detected via K_SERVICE env var
# CLOUDRUN_PROVIDER_DEV_MODE=true

# ============================================
# Advanced Configuration
# ============================================

# Output file path (default: /etc/traefik/dynamic/routes.yml)
# Can also be specified as first command argument
# OUTPUT_FILE=/tmp/routes.yml

# Poll interval for continuous mode (not currently used in one-shot mode)
# POLL_INTERVAL=30s

# ============================================
# Example Configurations
# ============================================

# Single project setup:
#   LABS_PROJECT_ID=my-project-prod
#   REGION=us-central1
#   ENVIRONMENT=prod

# Multi-project setup (e-skimming-labs style):
#   LABS_PROJECT_ID=labs-stg
#   HOME_PROJECT_ID=labs-home-stg
#   REGION=us-central1
#   ENVIRONMENT=stg

# Local development:
#   LABS_PROJECT_ID=my-project-stg
#   REGION=us-central1
#   ENVIRONMENT=dev
#   LOG_LEVEL=DEBUG
#   LOG_FORMAT=text
#   CLOUDRUN_PROVIDER_DEV_MODE=true
