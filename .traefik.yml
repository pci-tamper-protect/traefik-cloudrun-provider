# Traefik Plugin Manifest
# Note: This provider is currently a standalone binary, not a native Traefik plugin.
# This manifest is for future plugin conversion.

displayName: Cloud Run Provider
type: provider
iconPath: docs/icon.svg

summary: Dynamically discover Google Cloud Run services and generate Traefik routing configuration

testData:
  projectIDs:
    - test-project
  region: us-central1
  pollInterval: 30s
